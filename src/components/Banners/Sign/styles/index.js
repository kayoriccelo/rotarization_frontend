import { makeStyles } from '@material-ui/core/styles';


export default makeStyles({
    banner: {
        flex: 7,
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
    }
});
